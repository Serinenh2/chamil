# ---- Django -----------------------------------------------------------------
DJANGO_SECRET_KEY=changez-cette-cle-en-production
DJANGO_DEBUG=True
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1
TIME_ZONE=Africa/Algiers
LOG_LEVEL=INFO

# ---- Base de données (laisser vide pour utiliser SQLite en développement) ----
DB_NAME=chamil
DB_USER=chamil
DB_PASSWORD=motdepasse
DB_HOST=localhost
DB_PORT=5432

# ---- JWT --------------------------------------------------------------------
JWT_ACCESS_MINUTES=30
JWT_REFRESH_DAYS=7

# ---- CORS / Frontend --------------------------------------------------------
CORS_ALLOWED_ORIGINS=http://localhost:5173
FRONTEND_URL=http://localhost:5173

# ---- Celery / Redis ---------------------------------------------------------
CELERY_BROKER_URL=redis://localhost:6379/0
CELERY_RESULT_BACKEND=redis://localhost:6379/1
REDIS_CACHE_URL=redis://localhost:6379/2

# ---- E-mail -----------------------------------------------------------------
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.exemple.dz
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=no-reply@chamil.dz
EMAIL_HOST_PASSWORD=
DEFAULT_FROM_EMAIL=CHAMIL <no-reply@chamil.dz>
